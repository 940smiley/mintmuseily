## 2025-05-15 - [Accessible Form Patterns]
**Learning:** Adding semantic labels and ARIA attributes to Web3 minting forms significantly improves accessibility and screen reader support, which is often overlooked in dApps.
**Action:** Always associate inputs with `<label>` using `htmlFor` and add `aria-label` to transaction buttons.
